mixin rich-checkbox-buttons(opts)
  .rich-checkbox-buttons
    if(opts.title)
      .rich-checkbox-buttons__title= (opts.title)
    ul.rich-checkbox-buttons__list.checkbox-list
      each item in opts.items
        li.rich-checkbox-buttons__item.item-checkbox
          +item-checkbox(item)

//- +rich-checkbox-buttons({
//-   title: 'rich checkbox buttons',
//-   items: [
//-     {text: 'Широкий коридор', subtext: 'Ширина коридоров в номере не менее 91 см.'},
//-     {text: 'Помощник для инвалидов', subtext: 'На 1 этаже вас встретит специалист  и проводит до номера.'},
//-   ]
//- })